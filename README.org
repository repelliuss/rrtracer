1. Run ~make~ to build, an executable named ~rrtracer~ will be compiled.
2. Run ~rrtracer~ to get a ppm file named ~frame.ppm~
3. If you want ~compile_commands.json~, install ~bear~ and run ~bear -- make~.
